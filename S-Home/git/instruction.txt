自分のファイルをgithubに反映する方法

1. 新しく作った場合
a.まず、githubのサイトを開いてログインする

b.次に新しいリポジトリを作る

c.するとgithub上に以下のコードが出るので、github上に反映したいファイルをターミナルで開いてgithub上に書いてる以下のコードを一行ずる実行する



Quick setup — if you’ve done this kind of thing before
or	
	
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.
…or create a new repository on the command line
echo "# Sfront-r" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/SaikiadKohshi/Sfront-r.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/SaikiadKohshi/Sfront-r.git
git branch -M main
git push -u origin main

d.これで完了


2. 更新する場合
           Vsコード上で変更したいコードを含むファイルを選択する
　　　 変更したファイルを選択したら開発しているディレクトリ上で以下のコードを順番に打つ
　　　　git add . 
       git commit -m "first commit"
　　　　git push -u origin main
　　　これで変更した部分がgit hub上に反映されたかつ草も生えて完了






